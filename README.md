```
usage: gscholartrend.py [-h] [--since SINCE] [--to TO] [--plot]
                        [--csv filename]
                        keywords [keywords ...]

positional arguments:
  keywords        Keywords to search

optional arguments:
  -h, --help      show this help message and exit
  --since SINCE   Starting year
  --to TO         Ending year
  --plot          Plot the result
  --csv filename  Filename of CSV output result
```
